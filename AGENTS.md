# 저장소 작업 규칙

이 저장소를 수정하는 사람과 Codex는 다음 규칙을 지킨다.

1. 한국어 콘텐츠를 기본으로 한다.
2. 기존에 확정된 결정은 명시적인 새 합의 없이 임의로 뒤집지 않는다.
3. 결론을 바꿀 때는 변경 날짜와 이유를 `docs/decisions.md`에 남긴다.
4. `docs/itinerary.md`의 날짜별 일정과 `docs/reservations.md`의 예약 상태가 서로 모순되지 않게 유지한다.
5. 여행지 운영시간, 휴관일, 교통편처럼 변경 가능한 정보는 확인일과 공식 출처 링크를 함께 기록한다.
6. 확인하지 않은 정보는 확정 사실처럼 쓰지 않고 `검토 중` 또는 `검증 필요`로 표시한다.
7. 가족 개인정보, 예약번호, 실제 숙소명·상세주소·출입정보, 결제정보를 저장하지 않는다.
8. 콘텐츠 수정 후 `mkdocs build --strict`를 실행한다.
9. 사이트 구조와 모바일 가독성을 유지한다. 넓은 표보다는 짧은 목록이나 카드 구성을 우선한다.
10. 외부 이미지를 무단 핫링크하지 않으며, 새 외부 링크는 새 탭에서 열리도록 한다.
11. 외부 조사를 수행한 경우 Pages 문구만 수정하지 말고 관련 `research/*.md`에 근거를 남긴다.
12. 공식 사실, 과거 사례, 추론, 미확인을 명확히 구분한다.
13. 공식 출처가 없는 관행을 공식 규칙처럼 쓰지 않는다.
14. 변동 가능한 정보에는 확인일과 재확인일 또는 재확인 시점을 기록한다.
15. `docs/`에는 여행자가 실제로 실행할 결론만 간결하게 표시한다.
16. `research/`에는 판단 근거와 출처를 상세히 기록한다.
17. `docs/`, `research/`, `docs/decisions.md`, `docs/reservations.md`, `docs/checklist.md` 사이에 모순이 생기지 않게 한다.
18. 가격은 조사일 기준임을 표시하고 예약 시 다시 확인한다.
19. 외부 URL은 가능한 한 공식 기관의 원문을 사용하며 웹 검색 결과 요약이나 AI 답변 자체를 출처로 사용하지 않는다.
20. `research/`도 공개 저장소의 일부이므로 개인정보와 예약 세부정보를 기록하지 않는다.
21. 공개 관광지가 일정에 추가·삭제되거나 날짜·상태가 바뀌면 `docs/assets/map/places.geojson`을 같은 변경에서 갱신한다.
22. 지도에는 공개 관광지만 기록하고 실제 숙소 위치, 예약 식당, 가족 개인정보, 실시간 위치를 넣지 않는다.
23. 지도 좌표는 표시용으로 관리하며 입구·주차·운전 경로는 방문 전에 공식 안내와 Google Maps에서 다시 확인한다.
24. 지도 라이브러리 버전과 외부 스타일 URL은 명시적으로 고정하고, 저작자 표시와 장애 시 HTML 장소 목록을 유지한다.
25. Git 작업은 `main` 브랜치에서 직접 수행한다. 별도 브랜치를 만들거나 전환하거나 PR을 생성하지 않으며, 사용자가 명시적으로 요청한 경우에만 예외로 한다. 커밋·푸시 요청을 받으면 `main`에 바로 커밋하고 `origin/main`으로 푸시한다.

## 변경 전 확인

- 기존 결정을 `docs/decisions.md`에서 확인한다.
- 날짜, 요일, 숙박 수가 전체 일정과 맞는지 확인한다.
- 공개 저장소에 금지된 정보가 포함되지 않았는지 검색한다.
- 새 문서는 `mkdocs.yml`의 `nav`에 연결하고 내부 링크를 확인한다.
- `research/` 문서는 Pages에 공개할 문서가 아니므로 `mkdocs.yml`의 `nav`에 추가하지 않는다.
- 조사 결과를 반영할 때 `research/`의 근거와 `docs/`의 실행 결론이 일치하는지 확인한다.
- 공개 관광지 변경 시 `docs/assets/map/places.geojson`의 날짜·설명·상태가 일정 문서와 일치하는지 확인한다.
- 지도에는 숙소·예약 식당·개인정보가 포함되지 않았는지 확인한다.
